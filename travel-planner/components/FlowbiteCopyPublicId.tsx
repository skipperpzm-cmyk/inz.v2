"use client";
import React from "react";

type Props = { publicId?: string };

// Flowbite copy component removed — placeholder to avoid import errors.
export default function FlowbiteCopyPublicId(_props: Props) {
  return null;
}
