// CopyPublicId component removed — no-op placeholder to avoid leftover imports.
"use client";
import React from "react";

type Props = { publicId?: string };

export default function CopyPublicId(_props: Props) {
  return null;
}
