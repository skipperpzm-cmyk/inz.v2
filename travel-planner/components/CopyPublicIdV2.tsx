// CopyPublicIdV2 removed — no-op placeholder to avoid leftover imports.
"use client";
import React from "react";

type Props = { publicId?: string };

export default function CopyPublicIdV2(_props: Props) {
  return null;
}
